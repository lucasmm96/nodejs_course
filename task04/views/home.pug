doctype html
html(lang="en")
  include includes/head.pug
  body
    include includes/nav.pug
    .page-content
      h1 Home
      .page-form
        form(action='/add-user' method='POST')
          h3 User Registry:
          label.form-item(for="name") Username:
          input.form-item(type="text" name='name')
          button.form-item(type='submit') Submit